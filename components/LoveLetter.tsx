'use client'; import {motion,AnimatePresence} from 'framer-motion'; import {useState} from 'react'; import {content} from '@/content'; import Reveal from './Reveal';
export default function LoveLetter(){const [open,setOpen]=useState(false);return <section className="relative z-10 px-5 py-28"><Reveal className="mx-auto max-w-3xl text-center"><p className="section-kicker">a letter for you</p><h2 className="mt-4 font-serif text-5xl text-deep-rose md:text-7xl">The words I keep choosing</h2>{!open&&<motion.button whileHover={{scale:1.03}} whileTap={{scale:.98}} onClick={()=>setOpen(true)} className="focus-ring mt-12 min-h-12 rounded-full border border-rose/30 bg-rose px-7 text-sm font-medium text-white shadow-card">Open the letter ✉</motion.button>}<AnimatePresence>{open&&<motion.article initial={{opacity:0,y:50,rotateX:-8}} animate={{opacity:1,y:0,rotateX:0}} exit={{opacity:0,y:30}} transition={{duration:.9,ease:[.22,1,.36,1]}} className="paper-lines relative mt-12 rounded-[1.5rem] border border-gold/20 bg-paper p-7 text-left shadow-soft md:p-14"><div className="font-serif text-[1.22rem] leading-[1.75] text-ink/85 md:text-[1.35rem]">{content.letter.map((p,i)=><p key={i} className="mb-7 last:mb-0">{p}</p>)}</div><button onClick={()=>setOpen(false)} className="focus-ring mt-9 min-h-11 text-sm font-medium text-rose underline decoration-blush underline-offset-4">Fold it closed</button></motion.article>}</AnimatePresence></Reveal></section>}
