'use client'; import Image from 'next/image'; import {AnimatePresence,motion} from 'framer-motion'; import {useState} from 'react'; import {content} from '@/content'; import Reveal from './Reveal';
export default function Miu(){const [pet,setPet]=useState(0);return <section className="relative z-10 px-5 py-28"><Reveal className="mx-auto grid max-w-6xl items-center gap-12 rounded-[2rem] border border-white bg-white/55 p-6 shadow-soft backdrop-blur md:grid-cols-2 md:p-12"><div><p className="section-kicker">household management</p><h2 className="mt-4 font-serif text-6xl text-deep-rose md:text-8xl">Meet Miu <span className="text-4xl">🐾</span></h2><p className="mt-6 max-w-xl leading-8 text-ink/68">{content.miu.message}</p><motion.button whileHover={{rotate:[0,-3,3,0]}} whileTap={{scale:.95}} onClick={()=>setPet(x=>x+1)} className="focus-ring mt-8 min-h-12 rounded-full border border-rose/25 bg-ivory px-6 text-sm font-medium text-deep-rose shadow-card">Pet Miu gently</motion.button><AnimatePresence mode="popLayout">{pet>0&&<motion.span key={pet} initial={{opacity:0,y:8,scale:.8}} animate={{opacity:1,y:0,scale:1}} exit={{opacity:0,y:-8}} className="ml-4 inline-block font-serif text-2xl italic text-rose">{pet%3===0?'purrrr~':pet%2===0?'mrrp! ✨':'mrrp~'}</motion.span>}</AnimatePresence></div><div className="grid grid-cols-2 gap-3"><motion.div whileHover={{rotate:-2,y:-4}} className="relative col-span-2 aspect-[16/9] overflow-hidden rounded-2xl"><Image src={content.miu.photos[0]} alt="Miu, the beloved white cat" fill sizes="(max-width:768px) 100vw,50vw" className="object-cover"/></motion.div>{content.miu.photos.slice(1).map((src,i)=><motion.div whileHover={{rotate:i?2:-2,y:-4}} key={src} className="relative aspect-square overflow-hidden rounded-2xl"><Image src={src} alt={`Miu memory ${i+2}`} fill sizes="25vw" className="object-cover"/></motion.div>)}</div></Reveal></section>}
