'use client'; import Image from 'next/image'; import {AnimatePresence,motion} from 'framer-motion'; import {useState} from 'react'; import {content} from '@/content'; import Reveal from './Reveal';
export default function Gallery(){const [selected,setSelected]=useState<number|null>(null);return <section className="relative z-10 px-5 py-28"><div className="mx-auto max-w-6xl"><Reveal className="mb-14 text-center"><p className="section-kicker">our little archive</p><h2 className="mt-4 font-serif text-5xl text-deep-rose md:text-7xl">Us, in soft focus</h2><p className="mx-auto mt-4 max-w-xl text-ink/60">Nine frames for now. A thousand more still to come.</p></Reveal><div className="columns-1 gap-5 sm:columns-2 lg:columns-3">{content.photos.map((photo,i)=><motion.button initial={{opacity:0,y:24}} whileInView={{opacity:1,y:0}} viewport={{once:true}} whileHover={{y:-5,rotate:i%3===1?1:-.5}} transition={{duration:.65}} key={photo.src} onClick={()=>setSelected(i)} className="focus-ring mb-5 block w-full break-inside-avoid overflow-hidden rounded-2xl border border-white/80 bg-white p-2 text-left shadow-card"><div className={`relative overflow-hidden rounded-xl ${i===2?'aspect-[2/3]':i%4===0?'aspect-[4/5]':'aspect-[4/3]'}`}><Image src={photo.src} alt={photo.alt} fill sizes="(max-width:640px) 100vw,(max-width:1024px) 50vw,33vw" className="object-cover transition duration-700 hover:scale-105"/></div><p className="px-3 py-3 font-serif text-lg italic text-ink/70">{photo.caption}</p></motion.button>)}</div></div><AnimatePresence>{selected!==null&&<motion.div role="dialog" aria-modal="true" aria-label="Photo lightbox" initial={{opacity:0}} animate={{opacity:1}} exit={{opacity:0}} onClick={()=>setSelected(null)} className="fixed inset-0 z-[70] flex items-center justify-center bg-ink/75 p-5 backdrop-blur-sm"><motion.div initial={{scale:.95}} animate={{scale:1}} onClick={e=>e.stopPropagation()} className="relative w-full max-w-4xl rounded-2xl bg-ivory p-3 shadow-soft"><button aria-label="Close lightbox" onClick={()=>setSelected(null)} className="focus-ring absolute right-4 top-4 z-10 grid h-11 w-11 place-items-center rounded-full bg-ivory/90 text-xl text-deep-rose">×</button><div className="relative aspect-[4/3] overflow-hidden rounded-xl"><Image src={content.photos[selected].src} alt={content.photos[selected].alt} fill sizes="90vw" className="object-contain"/></div><p className="p-3 text-center font-serif text-xl italic">{content.photos[selected].caption}</p></motion.div></motion.div>}</AnimatePresence></section>}
